RN50: 
  embed_dim : 768
  image_resolution : 120
  vision_layers : [3, 4, 6, 3]
  vision_width : 256  # 256 in CLIP
  vision_patch_size : 0 # ideally it should be none
  context_length : 90
  vocab_size : 49408
  transformer_width : 512 #512 in CLIP
  transformer_heads : 8
  transformer_layers : 8  # 12 in CLIP


